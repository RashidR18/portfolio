{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "description": "A full-stack portfolio application with React frontend and Node.js backend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "backend": "npm run --workspace=backend",
    "frontend": "npm run --workspace=frontend"
  },
  "keywords": [
    "portfolio",
    "react",
    "node",
    "express"
  ],
  "author": "",
  "license": "MIT"
}
